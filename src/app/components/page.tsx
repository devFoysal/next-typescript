import React from 'react'

const page = () => {
  let abc:string = "hello ${abc} hello ${dev}";
  let abcd = {
    name: "hello"
  }
  let dd = "hello";
  console.log("🚀 ~ file: page.tsx:9 ~ page ~ dd:", dd)

  // TODO: this is should be replaced after 
  return (
    <div>page</div>
  )
}

export default page